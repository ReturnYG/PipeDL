# Archived Python version

`python/` preserves the pre-migration implementation, including existing local edits to `pipedl/app.py`. It is not built, installed, or invoked by the current application. Its CLI and installer are historical rollback references only.

Do not run it against a data directory currently owned by the Rust version. Follow `docs/migration.md` before a rollback.
